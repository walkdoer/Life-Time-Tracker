/**
 * @jsx React.DOM
 */

var React = require('react');
var Log = require('./Log');
var AddLog = React.createClass({
    render: function () {
        return (
            <div className="_c-addLog">
                <Log/>
            </div>
        );
    }
});

module.exports = AddLog;

React.render(
  <AddLog/>,
  document.getElementById('ltt-addLog')
);