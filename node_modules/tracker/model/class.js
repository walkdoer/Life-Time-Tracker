var mongoose = require('mongoose'),
    Schema = mongoose.Schema;

var Class = Schema({

    code: String,

    name: String,

    color: String,

    description: String
});

module.exports = mongoose.model('Class', Class);
