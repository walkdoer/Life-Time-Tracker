var mongoose = require('mongoose'),
    Schema = mongoose.Schema,
    ObjectId = Schema.ObjectId;

var Log = Schema({
    affect: {type: ObjectId},
    score: Number,
    createTime: Date
});

module.exports = mongoose.model('AffectRecord', Log);
