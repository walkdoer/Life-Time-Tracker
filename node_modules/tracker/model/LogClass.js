var mongoose = require('mongoose'),
    Schema = mongoose.Schema,
    ObjectId = Schema.ObjectId;

var LogClass = Schema({
    _id: String,
    name: String,
    color: String
});



module.exports = mongoose.model('LogClass', LogClass);
