/**
 * Version
 */
'use strict';

var numeral = require('numeral');

var Version = function (name, attrs) {
    this.name = name;
    this.attributes = attrs;
    var progress =  attrs.progress || attrs.pg;
    if (progress) {
        this.progress = numeral(progress).value();
    }
};

module.exports = Version;
