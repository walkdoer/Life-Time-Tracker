var mongoose = require('mongoose'),
    Schema = mongoose.Schema;

var Tag = Schema({
    name: String,
    lastActiveTime: Date,
    createTime: Date
});

module.exports = mongoose.model('Tag', Tag);
