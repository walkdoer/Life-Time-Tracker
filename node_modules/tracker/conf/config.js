var _ = require('lodash');
var defaultSettings = require('./defaultSettings.json');
var userSettings = require('./userSettings.json');

var sysConfig = {
    "defaultPerspectives": ["sport", "sit", "category", "mean"],
    "message": {
        "level": []
    },
    "reminders": {
        "plan": {
            "ahead": 10,
            "interval": 6,
            "aheadOfDone": 5
        },
        "drinkWater": {
            "logPath": "../logs/drankWater.log",
            "cupsOfOneDay": 8,
            "interval": 2400000
        }
    },
    "sync": {
        "evernote": {
            "interval": 3600000,
            "auto": true
        }
    }
};

module.exports = _.extend(sysConfig, defaultSettings, userSettings);