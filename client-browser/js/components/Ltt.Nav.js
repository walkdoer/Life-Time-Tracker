/**
 * @jsx React.DOM
 */

var React = require('react');

var Sidebar = React.createClass({

  /**
   * @return {object}
   */
  render: function() {
    return (
        <nav className="ltt_c-nav"> </nav>
    );
  }

});

module.exports = Sidebar;
